import { Modal } from "antd";
import styled from "styled-components";

export const AppFetchErrorWrapper = styled(Modal)` 
  border-radius: 12px;
  overflow: auto;
  padding-bottom: 0px;
  width: max-content !important;
`;